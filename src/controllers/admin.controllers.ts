import { Request, Response, NextFunction } from "express";

export const handleLogin = (
	req: Request,
	res: Response,
	next: NextFunction
) => {};

export const handleEditSlug = (
	req: Request,
	res: Response,
	next: NextFunction
) => {};

export const handleEditDestination = (
	req: Request,
	res: Response,
	next: NextFunction
) => {};
